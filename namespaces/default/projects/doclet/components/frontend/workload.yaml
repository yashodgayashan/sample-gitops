apiVersion: openchoreo.dev/v1alpha1
kind: Workload
metadata:
  name: frontend-workload
  namespace: default
spec:
  container:
    image: host.k3d.internal:10082/doclet-frontend-image:v1-e0458407
  dependencies:
    endpoints:
    - component: document-svc
      envBindings:
        address: DOC_SERVICE_URL
      name: http
      visibility: project
    - component: collab-svc
      envBindings:
        address: COLLAB_SERVICE_URL
      name: http
      visibility: project
  endpoints:
    http:
      port: 80
      type: HTTP
      visibility:
      - external
  owner:
    componentName: frontend
    projectName: doclet
